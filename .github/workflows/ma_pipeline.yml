name: Ma pipeline DevSecOps

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [main]

jobs:

  # JOB 1 — Secrets scanning

  secrets:
    name: Secrets Scanning (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # historique complet pour scanner tous les commits

      - name: Gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          #GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: github_pat_29837294ihefg290jihE320jih


  # # JOB 2 — Qualité du code

  # lint:
  #   name: Linting (Flake8)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'

  #     - name: Installer les dépendances
  #       run: pip install flake8

  #     - name: Lancer Flake8
  #       run: flake8 . --max-line-length=120 --exclude=__pycache__ || true


  # # JOB 3 — SAST (analyse statique)

  # sast:
  #   name: SAST (Bandit)
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'

  #     - name: Installer Bandit
  #       run: pip install bandit

  #     - name: Lancer Bandit
  #       run: bandit -r . -f json -o bandit-report.json || true

  #     - name: Afficher le rapport
  #       run: bandit -r . --severity-level medium

  #     - name: Sauvegarder le rapport
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: bandit-report
  #         path: bandit-report.json


  # # JOB 4 — SCA (dépendances)

  # sca:
  #   name: SCA (pip-audit)
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'

  #     - name: Installer pip-audit
  #       run: pip install pip-audit

  #     - name: Scanner les dépendances
  #       run: pip-audit -r requirements.txt



  # # JOB 5 — Build Docker

  # build:
  #   name: Build Docker Image
  #   runs-on: ubuntu-latest
  #   needs: [sast, sca, secrets]
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Login Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: Build et Push
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: true
  #         tags: ${{ secrets.DOCKERHUB_USERNAME }}/hackboard:${{ github.sha }}


  # # JOB 6 — Trivy (scan image Docker)

  # trivy:
  #   name: Trivy (Image Scan)
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Scanner l'image avec Trivy
  #       uses: aquasecurity/trivy-action@master
  #       with:
  #         image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/hackboard:${{ github.sha }}
  #         format: table
  #         severity: CRITICAL,HIGH
  #         exit-code: 0  # ne bloque pas la pipeline pour l'instant

  #     - name: Rapport Trivy en SARIF
  #       uses: aquasecurity/trivy-action@master
  #       with:
  #         image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/hackboard:${{ github.sha }}
  #         format: sarif
  #         output: trivy-report.sarif

  #     - name: Upload rapport GitHub Security
  #       uses: github/codeql-action/upload-sarif@v3
  #       with:
  #         sarif_file: trivy-report.sarif


  # # JOB 7 — DAST (ZAP)

  # dast:
  #   name: DAST (OWASP ZAP)
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Lancer l'app pour le scan
  #       run: |
  #         docker run -d \
  #           --name hackboard \
  #           -p 5000:5000 \
  #           ${{ secrets.DOCKERHUB_USERNAME }}/hackboard:${{ github.sha }}
  #         sleep 10  # attendre que l'app démarre

  #     - name: ZAP Baseline Scan
  #       uses: zaproxy/action-baseline@v0.12.0
  #       with:
  #         target: 'http://localhost:5000'
  #         rules_file_name: '.zap/rules.tsv'
  #         cmd_options: '-a'

  #     - name: Upload rapport ZAP
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: zap-report
  #         path: report_html.html
